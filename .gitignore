## .NET
bin/
obj/
*.user
.vs/
*.suo
project.lock.json
project.fragment.lock.json
artifacts/

## NuGet
*.nupkg
*.snupkg
nuget/

# VS/Rider per-user launch profiles
**/Properties/launchSettings.json
# IDEA Platform configurations
.idea/

# Test run output (coverage reports)
TestResults/

# Upstream sources kept locally for reference only: PipeWire, WirePlumber, GStreamer
# and anything else worth reading while working on the bindings. Cloned by
# build/fetch-upstream.sh, never committed - the pin that matters is generate/HEADER-VERSION.
external/
